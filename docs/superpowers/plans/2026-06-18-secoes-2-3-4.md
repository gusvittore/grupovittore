# Seções 2, 3 e 4 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Ajustar exclusivamente as seções 2, 3 e 4 da landing page para aproximá-las das referências aprovadas, preservando a Hero e as seções posteriores.

**Architecture:** Manter a estrutura atual da página e os gráficos em React/CSS/SVG. Aplicar classes específicas às três seções para evitar efeitos colaterais e usar um teste de contrato sobre o código-fonte para proteger os requisitos visuais objetivos.

**Tech Stack:** Next.js 16.2.9, React 19, TypeScript, Tailwind CSS 4, CSS global, SVG inline, Node.js test runner.

---

### Task 1: Contrato das seções

**Files:**
- Create: `tests/sections-2-3-4.test.mjs`

- [ ] Criar testes que exijam fundo off-white liso, cards sem ícones, painéis off-white, funil SVG simétrico, legenda sem quebra e classes próprias para a linha inferior.
- [ ] Rodar `node --test tests/sections-2-3-4.test.mjs` e confirmar falha no estado atual.

### Task 2: Seção 2

**Files:**
- Modify: `src/app/page.tsx`
- Modify: `src/app/globals.css`

- [ ] Remover os dois ícones informativos do JSX.
- [ ] Transformar os cards em blocos de texto com tipografia sans, maior legibilidade e fundo off-white.
- [ ] Aplicar fundo `#F8F4F0` sem imagem de grade somente à seção.

### Task 3: Seção 3

**Files:**
- Modify: `src/app/page.tsx`
- Modify: `src/app/globals.css`

- [ ] Reconstruir o gráfico de receita com eixos e labels alinhados no mesmo SVG.
- [ ] Substituir o funil em camadas CSS por um SVG simétrico.
- [ ] Dar classes individuais aos três painéis inferiores e redistribuir suas larguras.
- [ ] Impedir quebra interna das legendas, ampliar labels do radar e trocar todos os fundos internos para `#F8F4F0`.

### Task 4: Seção 4

**Files:**
- Modify: `src/app/globals.css`

- [ ] Remover a imagem/textura de fundo e manter o off-white sólido.
- [ ] Reforçar título e tipografia de apoio sem alterar copy, estátua ou composição geral.
- [ ] Trocar fundos claros internos para o mesmo off-white.

### Task 5: Verificação

**Files:**
- Verify: `src/app/page.tsx`
- Verify: `src/app/globals.css`

- [ ] Rodar o teste de contrato até passar.
- [ ] Rodar `npm run lint` e `npm run build`.
- [ ] Iniciar `npm run dev`, capturar desktop e mobile e inspecionar visualmente as seções 2–4.
- [ ] Comparar o trecho da Hero antes/depois e confirmar ausência de alterações.
